[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "newton"
version = "0.1.0"
description = "A simple implementation of newton's method"
dependencies = []
